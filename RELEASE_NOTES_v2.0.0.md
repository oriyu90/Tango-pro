# Tango pro v2.0.0

> [!WARNING]
> Android版は既公開版と互換の署名証明書、macOS版はad-hoc署名・未Notarizeです。更新前に学習記録ZIPを保存してください。

## 標準単語帳の刷新

- 中国語基本単語は継続して標準搭載
- 従来の標準英語7冊を配布資産から除外
- 出荷用フォルダのLv.1〜Lv.8英単語8冊・英熟語8冊を標準搭載
- 新規インストール時は合計17冊
- 既存ユーザーの自作・学習済み・旧標準単語帳は自動削除せず、v2標準単語帳だけを追加
- macOS保存データへ標準カタログ世代を追加し、旧保存形式から一度だけ安全に移行

## 表示と操作

- 長い単語帳名は従来サイズで測定し、収まらない場合は80%へ縮小。それでも収まらない場合だけ末尾を省略
- 設定にシンプルモードを追加
- シンプルモードは「選択中」を維持し、進捗をバーと%吹き出しへ集約
- 出題設定を開閉式に変更し、学習開始ボタンは常時表示
- シンプルモード中のダッシュボード文字を約10%拡大
- 問題文・4択を小／標準／大／特大の4段階で変更可能
- 4択ボタンを可変高にし、長い選択肢を最大3行で表示

## 周回表示

- 現在周回を「全単語の最小学習回数 + 1」で算出
- 全語を1回以上学習すると2周目、2回以上で3周目
- 2周目はティール、3周目はオレンジ、4周目はパープルの専用表示
- 5周目以降は追加の色変更をせず「x周目」だけを表示
- Androidは左ドロワーと選択中カード、macOSはサイドバーとダッシュボードに表示

## レスポンシブ対応

- Android狭幅では操作ボタン・チップ・範囲指定を折返し
- Android横長学習画面では問題と回答を左右配置
- タブレットでは本文最大幅を制限し、過度な横伸びを抑制
- 短い画面はスクロールして全選択肢へ到達可能
- macOSは最小ウィンドウを680×500へ緩和し、狭幅では4択を1列、広幅では2列表示

## 互換性

- Android applicationId `com.aistudio.vocabstudier.xwqnzy`、Room schema version 4を維持
- macOS保存形式version 1、学習記録ZIP format version 1を維持
- v1.2.xの単語帳、成績、単語帳別出題設定、学習記録ZIPを継続利用可能
- シンプルモードと文字サイズはアプリ共通設定として後方互換のある任意値で保存

## 配布物

- `Tango-pro-2.0.0-android.apk`: v1.2.0公開APKと同じ証明書で署名したrelease build
- `Tango-pro-2.0.0-universal.dmg`: Apple Silicon / Intel対応、ad-hoc署名、未Notarize
- `CHECKSUMS_v2.0.0.txt`: SHA-256

## 確認済み項目

- Android unit / Robolectric UI test 31件
- 狭幅360×640dp、横長960×480dp、特大文字・複数行4択
- Android Lint errors 0
- macOS Core self-test、旧保存データ移行、17冊初期投入
- Android / macOS build、署名、アーキテクチャ、成果物整合性

正式配布へ移行する場合は、Android正式署名鍵での更新インストール、実端末TTS、macOS Developer ID署名・Notarizationを別途確認してください。
